# 视频转文本工具 - 环境配置指南

## 一、项目结构

```
hacker2-x-profit/
├── src/
│   ├── server.js           # Express 服务器入口
│   └── services/
│       ├── videoToText.js  # 视频转文本服务
│       ├── whisper_service.py  # Whisper 语音识别
│       ├── aiService.js    # AI 文本处理服务
│       └── douyinParser.js # 抖音视频解析
├── public/
│   └── index.html         # 前端页面
├── temp/                  # 临时文件目录
│   ├── video/             # 临时视频
│   └── audio/             # 临时音频
├── package.json           # Node.js 依赖
├── .env                   # 环境变量配置
└── 配置文档.md            # 本文档
```

## 二、依赖列表

### 1. Node.js 依赖

```bash
npm install
```

`package.json` 内容：
```json
{
  "name": "hacker2-x-profit",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "openai": "^4.28.0",
    "dotenv": "^16.4.5",
    "cors": "^2.8.5",
    "uuid": "^9.0.1",
    "axios": "^1.6.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
```

### 2. Python 依赖

```bash
pip install -r requirements.txt
```

`requirements.txt` 内容：
```
openai-whisper==20231117
ffmpeg-python==0.2.0
srt==3.5.3
python-dotenv==1.0.1
zhconv>=1.4.3
```

### 3. FFmpeg（必须）

FFmpeg 是视频处理的核心工具，用于从视频中提取音频。

#### Windows 安装步骤：

1. 访问 https://ffmpeg.org/download.html#windows
2. 下载 `ffmpeg-release-essentials.zip`
3. 解压到指定目录，如 `D:\ffmpeg\ffmpeg\ffmpeg`
4. 将 `D:\ffmpeg\ffmpeg\ffmpeg\bin` 添加到系统环境变量 PATH
5. 重启终端使配置生效

**验证安装：**
```bash
ffmpeg -version
```

#### Linux/Ubuntu 安装步骤：

```bash
sudo apt update
sudo apt install -y ffmpeg
```

**验证安装：**
```bash
ffmpeg -version
```

### 4. Whisper 模型

Whisper 会在首次运行时自动下载默认模型（base，约140MB）。

**手动下载（可选）：**

如果需要手动管理模型：
1. 从 https://huggingface.co/openai/whisper-base 下载模型
2. 放到项目 `models/whisper/` 目录
3. 在 `.env` 中配置 `WHISPER_MODEL_PATH=models/whisper/base.pt`

## 三、环境配置

### 1. 创建 .env 文件

在项目根目录创建 `.env` 文件：

**Windows：**
```ini
# 端口配置
PORT=3001

# FFmpeg 路径（必须）
FFMPEG_PATH=D:\ffmpeg\ffmpeg\ffmpeg\bin

# Whisper 模型配置
WHISPER_MODEL_SIZE=base
WHISPER_MODEL_PATH=None
WHISPER_LANGUAGE=zh

# AI API 配置（火山引擎）
VOLCANO_API_KEY=your_api_key_here
VOLCANO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
AI_MODEL=Doubao-Seed-2.0-lite
```

**Linux 服务器：**
```ini
# 端口配置
PORT=3001

# FFmpeg 路径（必须）
FFMPEG_PATH=/usr/local/bin

# Whisper 模型配置
WHISPER_MODEL_SIZE=base
WHISPER_MODEL_PATH=models/whisper/base.pt
WHISPER_LANGUAGE=zh

# AI API 配置（火山引擎）
VOLCANO_API_KEY=your_api_key_here
VOLCANO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
AI_MODEL=Doubao-Seed-2.0-lite
```

### 2. 配置说明

| 配置项 | 说明 | Windows 示例 | Linux 服务器示例 |
|--------|------|--------------|------------------|
| `PORT` | 服务端口 | `3001` | `3001` |
| `FFMPEG_PATH` | FFmpeg bin目录 | `D:\ffmpeg...` | `/usr/local/bin` |
| `WHISPER_MODEL_SIZE` | Whisper模型大小 | `base` | `base` / `small` / `medium` |
| `WHISPER_MODEL_PATH` | 模型本地路径 | 可选 | 推荐配置 |
| `WHISPER_LANGUAGE` | 转写语言 | `zh` | `zh` |
| `VOLCANO_API_KEY` | AI API密钥 | 必填 | 必填 |
| `VOLCANO_BASE_URL` | AI API地址 | 必填 | 必填 |
| `AI_MODEL` | AI模型名称 | `Doubao-Seed-2.0-lite` | `Doubao-Seed-2.0-lite` |

## 四、启动项目

### 本地启动（Windows）

```bash
# 1. 进入项目目录
cd d:/gitlab/hacker2-x-profit

# 2. 安装 Node.js 依赖
npm install

# 3. 安装 Python 依赖
pip install -r requirements.txt

# 4. 启动服务
npm start
```

访问 http://127.0.0.1:3001/

### 开发模式（热重载）

```bash
npm run dev
```

### 服务器部署（Linux）

#### 1. 安装系统依赖

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install -y python3-pip ffmpeg nodejs npm

# CentOS/RHEL
sudo yum install -y python3-pip ffmpeg nodejs npm
```

#### 2. 安装 Node.js 依赖

```bash
npm install
```

#### 3. 安装 Python 依赖

```bash
pip install -r requirements.txt
```

#### 4. 配置 .env 文件

参考上方 Linux 服务器配置示例

#### 5. 使用 PM2 部署

```bash
# 安装 PM2
npm install -g pm2

# 启动服务
pm2 start src/server.js --name video2text

# 保存进程列表
pm2 save

# 设置开机自启
pm2 startup
```

**PM2 管理命令：**
```bash
pm2 list              # 查看进程列表
pm2 logs video2text   # 查看日志
pm2 restart video2text   # 重启服务
pm2 stop video2text   # 停止服务
```

#### 6. 配置 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your_domain.com;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置（视频处理需要较长时间）
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
        proxy_send_timeout 300s;
    }
}
```

```bash
sudo nginx -t
sudo systemctl restart nginx
```

## 五、功能说明

### 1. 视频转文本

- 支持上传本地视频文件
- 使用 FFmpeg 提取音频
- 使用 Whisper 进行语音识别
- 支持转换为简体字
- 生成 SRT 字幕文件

### 2. 抖音视频解析

- 支持粘贴抖音分享链接
- 自动提取真正的 URL
- 自动下载视频并转写
- 支持转换为简体字

### 3. AI 文本处理

- **文本优化**：对转写结果进行润色优化
- **文本总结**：根据问题总结视频内容
- 使用火山引擎豆包模型

## 六、API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `POST /api/upload_video` | 上传视频并转写 | multipart/form-data |
| `POST /api/parse_douyin` | 解析抖音链接并转写 | JSON |
| `POST /api/optimize_text` | AI 优化文本 | JSON |
| `POST /api/summarize_text` | AI 总结文本 | JSON |

## 七、常见问题

### 1. FFmpeg 找不到

- Windows：确认已将 FFmpeg bin 目录添加到 PATH，重启终端
- Linux：确认已安装 `ffmpeg`，使用 `which ffmpeg` 检查路径
- 或在 `.env` 中配置 `FFMPEG_PATH`

### 2. Whisper 模型下载慢

- 使用代理网络
- 或手动下载模型到 `models/whisper/` 目录
- 服务器部署建议手动下载模型

### 3. AI 功能无法使用

- 检查 `.env` 中的 `VOLCANO_API_KEY` 是否有效
- 检查服务器网络连接（可能需要配置代理）

### 4. 繁体转简体不工作

- 确认已安装 `zhconv`：`pip install zhconv`

### 5. 服务器视频处理超时

- 视频文件较大时，处理时间可能较长
- 建议 Nginx 配置中增加超时时间
- 或使用异步处理队列

### 6. 内存不足

- Whisper 模型占用内存较大
- 生产环境建议增加 swap 分区
- 或使用较小的模型（如 tiny/base）

### 7. Windows 上 Python 输出乱码

- 已添加 UTF-8 编码设置
- 确保 `.env` 文件保存为 UTF-8 编码

### 8. 端口被占用

```bash
# Windows 查找占用端口的进程
netstat -ano | findstr :3001

# 关闭进程
taskkill /F /PID <PID号>

# 或修改 .env 中的 PORT 为其他端口
```

## 八、目录权限

### Linux 服务器

```bash
# 创建必要目录
mkdir -p temp/video temp/audio

# 设置权限
chmod -R 755 temp/
chmod -R 755 public/
```