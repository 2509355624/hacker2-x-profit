# 视频转文本工具 - 环境配置指南

## 一、依赖列表

### 1. Python 依赖

```bash
pip install -r requirements.txt
```

`requirements.txt` 内容：
```
Django==4.2.15
openai-whisper==20231117
ffmpeg-python==0.2.0
srt==3.5.3
python-dotenv==1.0.1
gunicorn==21.2.0
aiohttp==3.9.1
lxml==5.1.0
openai>=1.0.0
requests>=2.31.0
zhconv>=1.4.3
```

### 2. FFmpeg（必须）

FFmpeg 是视频处理的核心工具，用于从视频中提取音频。

#### Windows 安装步骤：

1. 访问 https://ffmpeg.org/download.html#windows
2. 下载 `ffmpeg-release-essentials.zip`
3. 解压到指定目录，如 `D:\ffmpeg\ffmpeg\ffmpeg`
4. 将 `D:\ffmpeg\ffmpeg\ffmpeg\bin` 添加到系统环境变量 PATH
5. 重启终端使配置生效

**验证安装：**
```bash
ffmpeg -version
```

#### Linux/Ubuntu 安装步骤：

```bash
sudo apt update
sudo apt install -y ffmpeg
```

**验证安装：**
```bash
ffmpeg -version
```

#### Linux 手动安装（如果 apt 安装版本过低）：

```bash
# 下载
wget https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz
# 解压
tar -xf ffmpeg-release-amd64-static.tar.xz
# 移动到系统目录
sudo mv ffmpeg /usr/local/bin/
```

### 3. Whisper 模型

Whisper 会在首次运行时自动下载默认模型（base，约140MB）。

**手动下载（可选）：**

如果需要手动管理模型：
1. 从 https://huggingface.co/openai/whisper-base 下载模型
2. 放到项目 `models/whisper/` 目录
3. 在 `.env` 中配置 `WHISPER_MODEL_PATH=models/whisper/base.pt`

## 二、环境配置

### 1. 创建 .env 文件

在项目根目录创建 `.env` 文件：

**Windows：**
```ini
DJANGO_SECRET_KEY=your_secret_key_here
DEBUG=True
ALLOWED_HOSTS=*

WHISPER_MODEL_SIZE=base
WHISPER_MODEL_PATH=None
WHISPER_LANGUAGE=zh

FFMPEG_PATH=D:\ffmpeg\ffmpeg\ffmpeg\bin

DOUBAO_API_KEY=your_api_key_here
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
```

**Linux 服务器：**
```ini
DJANGO_SECRET_KEY=your_secret_key_here
DEBUG=False
ALLOWED_HOSTS=你的域名或IP

WHISPER_MODEL_SIZE=base
WHISPER_MODEL_PATH=models/whisper/base.pt
WHISPER_LANGUAGE=zh

FFMPEG_PATH=/usr/local/bin

DOUBAO_API_KEY=your_api_key_here
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
```

### 2. 配置说明

| 配置项 | 说明 | Windows 示例 | Linux 服务器示例 |
|--------|------|--------------|------------------|
| `DJANGO_SECRET_KEY` | Django密钥 | `dev_key...` | `生成随机密钥` |
| `DEBUG` | 调试模式 | `True` | `False` |
| `ALLOWED_HOSTS` | 允许访问的域名 | `*` | `example.com,IP` |
| `WHISPER_MODEL_SIZE` | Whisper模型大小 | `base` | `base` / `small` / `medium` |
| `WHISPER_MODEL_PATH` | 模型本地路径 | 可选 | 推荐配置 |
| `WHISPER_LANGUAGE` | 转写语言 | `zh` | `zh` |
| `FFMPEG_PATH` | FFmpeg bin目录 | `D:\ffmpeg...` | `/usr/local/bin` |
| `DOUBAO_API_KEY` | AI API密钥 | 必填 | 必填 |
| `DOUBAO_BASE_URL` | AI API地址 | 必填 | 必填 |

## 三、启动项目

### 本地启动（Windows）

```bash
# 1. 进入项目目录
cd d:/gitlab/hacker2-x-profit

# 2. 安装依赖
pip install -r requirements.txt

# 3. 启动服务
python manage.py runserver 8004
```

访问 http://127.0.0.1:8004/

### 服务器部署（Linux）

#### 1. 安装系统依赖

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install -y python3-pip ffmpeg

# CentOS/RHEL
sudo yum install -y python3-pip ffmpeg
```

#### 2. 创建虚拟环境

```bash
cd /path/to/project
python3 -m venv venv
source venv/bin/activate
```

#### 3. 安装 Python 依赖

```bash
pip install -r requirements.txt
```

#### 4. 配置 .env 文件

参考上方 Linux 服务器配置示例

#### 5. 收集静态文件

```bash
python manage.py collectstatic
```

#### 6. 启动服务

**开发环境测试：**
```bash
python manage.py runserver 0.0.0.0:8004
```

**生产环境（使用 Gunicorn）：**
```bash
pip install gunicorn
gunicorn video2text.wsgi:application --bind 0.0.0.0:8004 --workers 2 --threads 4
```

**后台运行：**
```bash
# 使用 nohup
nohup gunicorn video2text.wsgi:application --bind 0.0.0.0:8004 --workers 2 --threads 4 > gunicorn.log 2>&1 &

# 使用 systemd（推荐）
sudo nano /etc/systemd/system/video2text.service
```

**systemd 服务配置示例：**
```ini
[Unit]
Description=Video2Text Django App
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/path/to/project
Environment="PATH=/path/to/project/venv/bin"
ExecStart=/path/to/project/venv/bin/gunicorn video2text.wsgi:application --bind 127.0.0.1:8004 --workers 2 --threads 4
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable video2text
sudo systemctl start video2text
sudo systemctl status video2text
```

#### 7. 配置 Nginx 反向代理（可选）

```nginx
server {
    listen 80;
    server_name your_domain.com;

    location / {
        proxy_pass http://127.0.0.1:8004;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置（视频处理需要较长时间）
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
        proxy_send_timeout 300s;
    }

    location /static/ {
        alias /path/to/project/staticfiles/;
    }

    location /media/ {
        alias /path/to/project/media/;
    }
}
```

```bash
sudo nginx -t
sudo systemctl restart nginx
```

## 四、目录结构

```
django_video2text/
├── app/
│   ├── ai/              # AI服务（优化、总结）
│   ├── templates/       # 前端模板
│   ├── views.py         # 视图接口
│   ├── urls.py          # 路由
│   └── services.py      # 核心业务（视频转文本）
├── video2text/         # Django配置
├── utils/tools/         # 工具（抖音视频解析）
├── media/               # 临时文件目录
│   ├── temp_video/     # 临时视频
│   └── temp_audio/      # 临时音频
├── models/whisper/      # Whisper模型目录（可选）
├── .env                 # 环境变量配置
├── .gitignore
├── requirements.txt
└── manage.py
```

## 五、常见问题

### 1. FFmpeg 找不到
- Windows：确认已将 FFmpeg bin 目录添加到 PATH，重启终端
- Linux：确认已安装 `ffmpeg`，使用 `which ffmpeg` 检查路径
- 或在 `.env` 中配置 `FFMPEG_PATH`

### 2. Whisper 模型下载慢
- 使用代理网络
- 或手动下载模型到 `models/whisper/` 目录
- 服务器部署建议手动下载模型

### 3. AI 功能无法使用
- 检查 `.env` 中的 `DOUBAO_API_KEY` 是否有效
- 检查服务器网络连接（可能需要配置代理）

### 4. 繁体转简体不工作
- 确认已安装 `zhconv`：`pip install zhconv`

### 5. 服务器视频处理超时
- 视频文件较大时，处理时间可能较长
- 建议 Nginx 配置中增加超时时间
- 或使用异步处理队列

### 6. 内存不足
- Whisper 模型占用内存较大
- 生产环境建议增加 swap 分区
- 或使用较小的模型（如 tiny/base）
