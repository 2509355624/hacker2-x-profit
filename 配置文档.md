# 视频转文本与TTS语音合成工具

## 一、项目结构

```
hacker2-x-profit/
├── src/
│   ├── server.js              # Express 服务器入口
│   └── services/
│       ├── videoToText.js     # 视频转文本服务
│       ├── whisper_service.py # Whisper 语音识别
│       ├── aiService.js       # AI 文本处理服务
│       ├── douyinParser.js    # 抖音视频解析
│       └── tts_service.py     # TTS 语音合成服务
├── public/
│   └── index.html             # 前端页面
├── tests/
│   └── test_api_demo.py       # TTS API 测试脚本
├── temp/                      # 临时文件目录
│   ├── video/                 # 临时视频
│   ├── audio/                 # 临时音频
│   ├── ref_audio/             # 参考音频
│   └── aux_audio/             # 辅助音频
├── output_audio/              # 生成的音频文件
├── package.json               # Node.js 依赖
├── .env                       # 环境变量配置（不上传）
└── 配置文档.md                 # 本文档
```

## 二、功能特性

### 1. 视频转文本
- 支持上传本地视频文件
- 使用 FFmpeg 提取音频
- 使用 Whisper 进行语音识别
- 支持转换为简体字
- 生成 SRT 字幕文件

### 2. 抖音视频解析
- 支持粘贴抖音分享链接
- 自动提取真正的 URL 并下载
- 自动转写为文本

### 3. TTS 语音合成
- 基于 GPT-SoVITS 的语音克隆合成
- 支持批量分段合成
- 支持音频文件合并
- 可调节语速、采样步数等参数

### 4. AI 文本处理
- 文本优化润色
- 文本总结

## 三、依赖列表

### 1. Node.js 依赖

```bash
npm install
```

### 2. Python 依赖

```bash
pip install -r requirements.txt
```

requirements.txt 内容：
```
openai-whisper==20231117
ffmpeg-python==0.2.0
srt==3.5.3
python-dotenv==1.0.1
zhconv>=1.4.3
gradio_client
```

### 3. FFmpeg（必须）

从 https://ffmpeg.org/download.html 下载并配置

### 4. GPT-SoVITS API

TTS 功能需要运行 GPT-SoVITS 服务：
```bash
cd D:\My_AI_project\Sound_extraction\GPT-SoVITS-v3
python webui.py --port 9872
```

## 四、环境配置

创建 `.env` 文件：

```ini
# 服务端口
PORT=3001

# FFmpeg 路径（必须）
FFMPEG_PATH=D:\ffmpeg\ffmpeg\bin

# Python 路径
PYTHON_BIN=D:\AI\confyui_env\Scripts\python.exe

# Whisper 模型配置
WHISPER_MODEL_SIZE=base
WHISPER_LANGUAGE=zh

# AI API 配置（火山引擎）
VOLCANO_API_KEY=your_api_key_here
VOLCANO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
AI_MODEL=Doubao-Seed-2.0-pro
```

## 五、启动项目

```bash
# 1. 进入项目目录
cd d:/gitlab/hacker2-x-profit

# 2. 安装依赖
npm install
pip install -r requirements.txt

# 3. 启动 GPT-SoVITS API 服务（端口 9872）
# 参考上方第四节

# 4. 启动本项目服务
npm start

# 5. 访问 http://127.0.0.1:3001/
```

## 六、API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `POST /api/upload_video` | 上传视频并转写 | multipart/form-data |
| `POST /api/parse_douyin` | 解析抖音链接并转写 | JSON |
| `POST /api/optimize_text` | AI 优化文本 | JSON |
| `POST /api/summarize_text` | AI 总结文本 | JSON |
| `POST /api/tts` | TTS 语音合成 | JSON |
| `POST /api/tts/merge` | 合并多个音频文件 | JSON |
| `POST /api/audio/upload` | 上传音频文件 | multipart/form-data |

## 七、TTS 功能说明

### 基本 TTS
1. 选择参考音频
2. 勾选"不使用参考文本"（推荐）
3. 点击"朗读转写内容"

### 批量 TTS
1. 设置分段长度（100-700字符）
2. 点击"分段朗读全部文本"
3. 等待所有段落处理完成
4. 点击"合并所有音频"
5. 可下载合并后的完整音频

### 音频合并
1. 在"音频文件合并"区域
2. 选择带编号的音频文件（1.wav, 2.wav...）
3. 点击"上传并合并"
4. 自动按编号排序并合并

## 八、常见问题

### 1. TTS 合成失败
- 确认 GPT-SoVITS API 服务运行在 http://localhost:9872/

### 2. FFmpeg 找不到
- 确认已将 FFmpeg bin 目录添加到 PATH
- 或在 `.env` 中配置 `FFMPEG_PATH`

### 3. 内存不足
- Whisper 模型占用内存较大
- 使用较小的模型（如 tiny/base）

### 4. 端口被占用
```bash
netstat -ano | findstr :3001
# 或修改 .env 中的 PORT
```
